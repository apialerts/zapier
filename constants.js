const BASE_URL = 'https://api.apialerts.com';
const OAUTH_URL = 'https://oauth.apialerts.com';
const INTEGRATION = 'zapier';
const VERSION = require('./package.json').version;

module.exports = { BASE_URL, OAUTH_URL, INTEGRATION, VERSION };
